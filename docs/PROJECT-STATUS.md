# 项目状态

## 2026-08-18 七渠道实现候选、PRD 与原型同步

| 门禁 | 当前状态 | 证据 |
| --- | --- | --- |
| P1 产品基线 | V1.3.2-draft，常规业务 PRD 已按最新范围更新，待用户 Review | `docs/PRODUCT.md` |
| PA1 项目适配基线 | PA1-v1-draft，待用户 Review | `docs/PROJECT-ADAPTATION.md` |
| I1 页面交互基线 | I1-V1.3-draft，待用户 Review | `docs/PAGE-INTERACTIONS.md` |
| P2 原型基线 | 探索原型已同步七渠道，待 P1/PA1/I1 确认后评审 | `design/aiops-platform-prototype.html`、`design/exports/aiops-platform/` |
| E1 本地实现 | 已完成 | Shared、内容引擎、API、Worker、Web、统一扩展 1.4.0 |
| T1 本地验证 | 通过；生产真实账号待验收 | `pnpm typecheck`、148 项单测、`pnpm build`、扩展安全检查、`pnpm test:isolated` |

- 当前代码正式包含官网、公众号、小红书、知乎、今日头条、百家号和 LinkedIn 七渠道；浏览器扩展覆盖后五个平台。
- LinkedIn 已接入独立内容模板、产物持久化、人工复核、统一扩展、多账号草稿和受控正式发布；扩展只在按钮和成功信号唯一时执行，否则安全停止。
- 定时任务可选择全部七渠道，Worker 强制先进入人工复核。本地隔离回归已验证 7 张复核单、官网/公众号 Mock 草稿、五个平台上传包及父任务 drafted 终态。
- 统一扩展升级为 1.4.0；新旧兼容下载包均已在本地构建并核验包含 LinkedIn 域名与同一版本。
- 已绘制 20 个核心 Frame；本轮同步七渠道、五平台和已确认决策。P1/PA1/I1/P2 仍需用户 Review，不能把探索原型标为确认基线。
- PRD 已从面向研发的规则索引重写为面向产品与运营评审的常规结构，按 18 个模块逐项说明页面目的、字段、用户操作、渠道差异、业务规则、失败反馈和验收；技术状态保留为后置章节，不再作为正文阅读主线。
- 当前 Git 为未提交、未推送、未部署。LinkedIn 从当前中国大陆网络访问公开文章入口会被重定向/限制；真实 DOM、账号身份、草稿和正式发布成功信号必须在可访问网络和真实账号中验收。

## 视觉质量证据

| VQ ID | Surface | 受众 | 模式 | 风险 | 内置核心 | 专家路由 | 品牌 | 层级 | 构图 | 工艺 | 响应式 | 状态 | 桌面证据 | 移动证据 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- | --- |
| VQ-AIOPS-V132 | AI 运营中心完整原型 | 内部 | Operate | L2 | 已执行 | Impeccable harden；Taste 不用于后台任务界面 | 18 | 18 | 17 | 13 | 12 | 10 | `design/exports/aiops-platform-complete/content-case.png`、`wechat-cover.png`、`channel-recovery.png` | `design/exports/aiops-platform/p0x3vO.png` | 通过 |

- Surface：内部运营后台 Operate，风险 L2；极客跳动红用于主操作、当前状态与风险提示，主体采用冷灰画布和白色任务工作面。
- 本地完整原型：`design/aiops-platform-prototype.html`；20 个核心 Frame 的最新 PNG 位于 `design/exports/aiops-platform/`。同目录 PDF 为上一轮归档版，本轮评审以 HTML 与 PNG 为准。Pencil 编辑源由编辑器托管，当前未在仓库物化。
- 已人工抽查任务生成、连续复核、渠道结果、资产、图片工坊、渠道管理、多账号、移动复核、扩展弹窗和覆盖地图；未发现明显坍塌、裁切或无意义横向滚动。
- 本轮新增 28 个独立详细 Frame，总原型达到 48 个顶层 Frame；已抽查案例创建、公众号组合封面、渠道安装恢复和既有移动复核，补齐旧覆盖地图中未实际绘制的页面与状态。
- Impeccable 静态检测对导出的核心评审 HTML 返回 0 项阻断问题。
- 因当前协作约束不允许启动独立子审查者，本轮按 degraded finish review 在主线程复查；正式 P2 前仍需用户 Review 和独立视觉复核。

更新时间：2026-08-18

## 2026-08-17 多账号草稿与正式发布（本地实现完成，待真实账号验收）

- 最新授权：四个浏览器渠道支持在不同 Chrome 配置文件或同事电脑登录账号后绑定到运营中心，一篇已复核文章可选择多个账号批量保存草稿，也可在二次确认后正式发布。
- 安全边界：不保存平台密码、Cookie 或登录态；默认草稿。正式发布授权绑定单个批次、审核内容指纹和明确账号清单，登录身份不一致、验证码、按钮不唯一或结果不明时停止且不自动重试。
- 兼容边界：官网、公众号和原单账号浏览器草稿流程不改；多账号能力使用独立投放批次与逐账号结果，不复用或覆盖原 `job_targets` 终态。
- 基线影响：P1/PA1/I1/P2/E1/T1/E2 因新增账号、投放批次、正式发布状态和页面而需复审；用户已明确授权直接实施，视觉继续复用现有内部 Operate L1 组件体系，不新增品牌方向。
- 已实现：新增独立账号绑定、投放批次和逐账号任务表；渠道管理可绑定四个平台当前登录账号；多账号发布页可选择已复核文章、渠道、多个账号与草稿/正式模式；扩展 `1.3.0` 每分钟领取仅属于当前连接的任务，并在写入前再次核对公开账号标识。
- 正式发布门禁：必须勾选已核对文章与配图、完整输入审核标题，服务端把确认人、内容指纹和账号清单写入一次性批次授权；扩展只允许点击唯一正式发布按钮，缺少成功信号则进入 `ambiguous` 且不自动重试。
- 本地验证：6 工作区类型检查、API 36 项测试、Worker 33 项测试、扩展安全测试、生产构建和隔离全流程回归均通过；隔离回归覆盖迁移重复执行、账号令牌隔离、批次幂等、单账号失败隔离、正式发布确认和指纹授权，原官网、公众号、小红书旧流程保持通过。
- 额外兼容修复：隔离回归发现旧图片工坊 Mock 融合向当前图片服务发送已失效的 `scale=0/position=auto`；Mock 回退改用服务默认缩放和居中，真实 AI 融合仍由模型按提示词和两张源图自动构图。
- 待验收：真实平台账号暂不可用；次日需逐个平台验证账号识别选择器、草稿按钮、正式发布按钮、验证码安全停止和平台成功信号。当前不能宣称真实正式发布闭环已通过。

## 2026-08-17 知乎、今日头条与百家号草稿适配

- 内容渠道扩展为官网、公众号、小红书、知乎文章、今日头条图文和百家号图文；新增三平台各自独立写作模板、质检、排版产物、人工复核与任务状态。
- 小红书扩展升级为 `1.2.0` 多平台草稿助手，复用现有个人电脑连接、指定任务领取、图片下载、心跳、结果回传与失败安全停止机制。下载包更名为 `geekdance-multi-platform-draft-uploader.zip`。
- 知乎 V1 只处理知乎文章，今日头条和百家号 V1 只处理图文；扩展禁止点击发布或提交审核。只有草稿按钮唯一明确时才自动点击，否则停在已填写状态交由人工保存。
- 本地已通过六工作区 TypeScript 检查、139 项单元测试、扩展安全检查、生产构建、ZIP 完整性检查和本地 PostgreSQL 迁移二次幂等执行。当前只能宣称代码适配和本地回归通过，不能宣称三平台真实草稿已验收。

## 2026-08-17 多平台扩展下载与渠道状态修复

- 扩展弹窗、默认设备名和运行状态统一改为“多平台草稿助手”，不再让知乎、今日头条和百家号用户误以为下载的是小红书专用包。
- Web 构建会把同一份 `1.2.0` 扩展文件同时写入新安装包和旧版兼容更新包；旧 URL 保留原内部目录名，旧扩展可原目录覆盖并保留 Chrome ID 与本机连接，避免新旧扩展同时运行、争抢网页消息。
- 渠道管理的四个扩展渠道改为同时检查服务端运行配置与当前电脑的安装、版本和连接状态；不再用团队中其他电脑的历史连接冒充当前电脑可用。Dashboard 的可用渠道分母同步从 3 修正为 6。
- 服务端健康状态按四个平台分别计算内容运行问题，通用问题码改为 `BROWSER_EXTENSION_NOT_CONNECTED`；真实账号与平台页面 DOM 仍需部署后逐个平台验收。
- 2026-08-17 线上只读核验：新下载地址仍返回 404，旧地址仍是 `1.1.17` 小红书专用包；说明本候选尚未部署，当前线上同事无法使用新增三平台。部署后必须再次核验两个 URL 均返回同一份 `1.2.0` 包。
- 旧功能兼容专项回归已在隔离 PostgreSQL、Redis、API、Worker 和当前图片服务镜像中通过：官网/公众号双渠道自动草稿、官网/公众号/小红书三渠道人工复核、旧 `/api/extensions/xiaohongshu/*` 路径、指定任务领取、心跳、草稿结果回传、正式发布禁用、扩展撤销均正常。回归脚本同步移除已经下线的两种拼接操作，改测自定义 Logo 叠加和人物背景自动融合。
- 生产健康检查会同时下载新安装包和旧版兼容更新包，并核验二者的版本与“多平台草稿助手”名称；不能再以只验证旧小红书 URL 的方式通过部署验收。

## 2026-08-18 图片成图字体真实性与字距

- 修复“SVG 声明阿里巴巴普惠体、最终成图却可能被 librsvg 静默替换”的问题：所有程序叠加文字统一通过 Resvg 渲染，并显式加载阿里巴巴普惠体 2.0 的 55 Regular 与 85 Bold TTF；生产环境关闭系统字体回退，缺少字体或像素验证失败时 Worker 会拒绝启动。
- 标题实际使用 85 Bold，正文实际使用 55 Regular；默认中文文字字距轻微增至 `0.03em`，已有单独品牌字距规则的 GeekDance 标识保持原设计，避免重复放大。
- 普通 4:3 AI 插图、案例结构图、小红书封面、公众号兜底封面和案例兜底封面均接入同一确定性字体渲染链路；持久化 SVG 同步写入字距，避免预览和最终 PNG 不一致。
- 根整合镜像与独立 Worker 镜像都会把固定哈希的 WOFF2 解压为 TTF，并修正品牌图片读取权限。两种生产镜像均已构建成功且通过真实字体像素验证。
- 本地验证通过：全仓类型检查、内容引擎 59 项、渠道适配器 15 项、API 37 项、Worker 34 项（共 145 项）测试、全仓生产构建、隔离全流程回归及 `git diff --check`。旧图片不会自动改版，部署后重新生成的图片才会使用新字体和字距。

## 2026-08-17 图片字体、内容驱动框架与公众号封面裁剪

- 程序绘制图片的字体族收紧为唯一的 `Alibaba PuHuiTi 2.0`，标题统一 700、正文统一 400；移除苹方、微软雅黑和 Noto 等 SVG 字体回退，公众号兜底封面同步修正。生产镜像仍通过 `fc-match` 验证普惠体文件。
- 文章插图从 8 种基础结构扩展为 14 种内容驱动视觉语法，新增时间轴、路线图、漏斗、系统架构、业务生态和行动清单。分类器根据转化、演进、规划、架构、角色、清单等正文信号选择框架，同一篇 6 张图继续强制去重，不再只替换卡片文字。
- 公众号真实封面生成后可分别调整 2.35:1 首图和 1:1 次图选区；裁剪基于上传原图，重新生成带渐变和 GeekDance 字标的完整拼图，并自动刷新公众号复核预览。
- 本地验证：6 个工作区类型检查通过，133 项测试通过，Web/API/Worker 等生产构建通过；生产 Worker Docker 镜像内 `fc-match` 已实际匹配阿里巴巴普惠体 Regular，并同时包含 Bold 字重。当前尚未提交、推送、部署或真实账号复验。

## 2026-08-17 字体依赖部署修复

- 流水线日志只保留了 `pnpm install --frozen-lockfile` 的最终退出位置，没有保留 pnpm 原始错误；结合本次新增依赖核查，阿里巴巴普惠体是唯一被强制绕开国内镜像、单独访问 `registry.npmjs.org` 的包，因此移除这条不稳定安装路径。
- Worker 不再把字体作为 Node 运行依赖安装。根整合镜像和独立 Worker 镜像均从固定版本 jsDelivr 地址下载 Regular/Bold 两个字体文件，启用网络重试，并以固定 SHA-256 校验文件完整性；独立 Worker 显式安装 CA 证书，避免 HTTPS 下载报 `curl: (77)`。
- 本地验证：根生产镜像以无缓存方式完整构建成功，原失败的冻结安装阶段通过；独立 Worker 镜像构建成功；两个最终容器均通过字体哈希与 `fc-match 'Alibaba PuHuiTi 2.0'` 验证。当前修复尚未提交、推送或部署。

## 2026-08-14 复核预览与图片工坊交互修复

- “预览当前修改”现在会提交当前复核稿、重新生成渠道 HTML、强制刷新 iframe、切换到正确渠道并滚动定位；按钮与就近状态区分别显示生成中、成功或失败，不再只是打开旧预览。
- 人物与背景融合取消固定人物位置和大小选项，模型会根据背景留白、透视、地面和光线自动构图；融合要求仍可覆盖自动判断。本地图片服务也使用背景留白分析完成自动位置和比例回归。
- 删除精确拼接与 AI 创意拼接的界面、共享操作枚举和执行端点；Logo 叠加改为底图与自定义 Logo 双素材，并提供真实预览内拖动定位和宽度调整。
- 小红书封面改字增加 OpenAI 视觉文字块识别、图上目标选择和边界微调；输出只修补并确定性重绘选中区域，保持原图尺寸和其他内容。复杂纹理下仍需运营核对识别框和修补效果。
- 图片工坊上传、素材读取、文字识别和任务提交，以及内容资产、定时任务、成员管理、任务删除与回收站操作补齐局部加载状态和成功/失败反馈。
- 本地验证：Python 图片服务语法检查通过，6 个工作区类型检查通过，131 项测试通过；尚未提交、推送、部署或使用生产 OpenAI 与真实封面复验。

## 2026-08-14 公众号复核、封面与上传修复

- 通识、行业和趋势文章新增软件定制获客方向：从需求、核心流程、MVP 验证和分阶段交付分析产品机会，质量门拦截无证据的“没有市场”类绝对结论；极客跳动的产品与开发视角自然进入开场和正文，不再只放在末尾。
- 普通文章插图标题改为语义关键词高亮，优先识别行业、产品、用户需求、MVP 和开发交付等核心词，不再固定标红行尾。
- 公众号复核上传真实封面后，立即执行 2.35:1 + 1:1 裁剪、渐变、GeekDance 字标和拼图，并自动刷新复核预览；通过复核后直接复用同一已品牌化资产，避免重复叠加渐变与字标。
- 公众号复核稿新增可编辑的 `01 / 02 / 03` 三条总结要点；历史文章默认从前三个章节生成，人工修改后进入预览、复核版本和最终公众号草稿。
- 公众号 2.35:1 与 1:1 拼图继续使用同一真实图片，两个裁剪顶部均叠加批准的透明 `GeekDance` 字标；字标横向扩展到画面宽度约 88%，红色渐变最高不透明度固定为 50%，其余色标逐步淡出，避免整幅遮挡主体。
- 公众号封面上传会在浏览器端将大图最长边压缩到 2400px、JPEG 质量 90%；服务端完成本地文件与素材记录后立即返回，数据库大图备份和 OSS 镜像转为后台执行，消除同步存储导致的 HTTP 524 风险。
- 本地验证：全仓 130 项测试通过，6 个工作区类型检查通过，生产构建通过，公众号封面样图已人工检查。当前修改尚未提交、推送或部署，生产环境仍需部署后用真实图片复验。

## 2026-08-14 小红书扩展恢复修复

- 生产真实账号复验发现扩展更新后旧创作页保留失效内容脚本，Chrome 报 `Extension context invalidated`，任务最终进入 `XHS_UPLOAD_PREPARE_FAILED`。
- 扩展 1.1.16 在打开创作页后先做内容脚本就绪握手；正常清单脚本未响应时，使用受限的 `scripting` 权限只向当前 `creator.xiaohongshu.com` 标签重新注入已审计的适配器与上传脚本。
- 该恢复只用于自动上传和填写，不点击“暂存离开”或正式发布；任务仍保留唯一 ID、草稿门禁和失败安全停止。

## 当前目标

在既有官网、公众号、小红书草稿链路上补齐人工复核闭环，并让每位运营同事可在自己的 Chrome 中安全连接小红书草稿助手，从运营中心指定任务一键保存草稿。

## 基线

| 门    | 状态       | 证据                                        |
| ----- | ---------- | ------------------------------------------- |
| P1-v1 | 已授权实施 | `docs/PRODUCT.md`，用户明确要求合并正式线上 |
| I1-v1 | 已授权实施 | `docs/PAGE-INTERACTIONS.md`                 |
| E1    | 已完成     | 复核表、API、审计、导航、详情操作与登录文案 |
| T1    | 已通过     | 全仓测试、生产构建、本地 API/浏览器闭环     |

## 视觉质量

- Surface：内部运营后台，Operate，L1。
- 复用现有组件与品牌令牌；不引入新的视觉体系。
- 登录页属于纯文案调整（L0）。
- 专家调用：跳过；原因是本次不改变既有布局、组件体系或品牌方向。

## 风险

- 人工确认草稿存在属于状态纠偏，必须审计且不得触发渠道调用。
- 渠道结果不明确不得自动重试。
- 官网与公众号正常自动草稿路径必须保持无条件兼容。

## 已完成实现

- 新增团队共享的 `/reviews` 人工复核列表，支持待处理与处理记录。
- 内容质量复核支持“通过并仅创建当前渠道草稿”和“驳回并停止当前渠道”。
- 渠道结果不确定支持“确认草稿已存在”和“确认不存在后安全重试”。
- 服务端强制校验决策类型、复核意见、产物完整性、HTTPS 地址、待处理状态与并发锁。
- `manual_reviews` 保留历史，一条渠道目标同时最多存在一条待处理复核。
- 实际复核人、意见、外部草稿信息和决定写入审计记录。
- 小红书人工确认草稿存在时同步 `xiaohongshu_upload_tasks`，仍禁止正式发布。
- 官网和公众号正常成功路径不创建复核；复核重试只把当前目标设为 `queued`，已成功目标保持 `drafted`。
- 登录页只完成正式化文案调整，认证行为未改动。

## 验证证据

- `pnpm typecheck`：6 个有类型检查脚本的工作区全部通过。
- `pnpm test`：内容引擎 43 项、渠道适配器 14 项、worker 9 项、API 5 项，共 71 项通过。
- `pnpm build`：Web、API、worker、共享包、内容引擎和渠道适配器生产构建全部通过；构建路由包含 `/reviews`。
- 本地 Docker API 启动并执行数据库迁移成功。
- 浏览器验证：正式化登录页、运营成员共享导航、人工复核空状态、两类复核列表、任务详情中的预览/质检/复核操作均正确渲染。
- 本地 API 闭环：内容驳回返回 `rejected`；确认草稿存在返回 `confirmed_drafted`，记录实际复核人且没有排队。
- 并发闭环：同一复核两个并发决定分别返回 HTTP 200/409，仅一次成功；目标与小红书上传任务均同步为 `drafted`。
- 本地验收使用的临时任务、复核记录、审计记录和临时运营账号均已清理。

## 发布前提示

- 本轮修复与既有小红书扩展、人工复核改动必须作为同一个部署候选发布；部署启动 API 后由幂等迁移创建 `manual_reviews`。
- 当前受保护的线上页面尚未完成真实账号闭环：内置浏览器标签页未附加到自动化会话，且本候选尚未部署。部署后需在已登录标签页完成一次官网、公众号和小红书草稿验收。
- 本地候选进一步修复小红书真实话题组件前的同名检索文字残留；扩展版本升级为 `1.1.15`，网页探测改为只读取本地安装与配对状态，不再等待服务端网络检查后才响应，降低网络延迟造成的“未安装”误判。普通文章插图使用内容驱动规划：先识别过程、差异、趋势、层级、循环、多维判断、关系或概念，每篇固定生成 6 张且同一种视觉框架最多使用一次；官网、公众号和小红书复用同一组 4:3 正文图，小红书另生成独立 3:4 竖版封面。官方 OpenAI `gpt-image-2` 保持 `high` 质量并改为全局最多 3 路并发，缩短等待但不降低分辨率或画质。模型结果会先降采样并柔化以不可逆消除任何偶发字形，最终标题、节点标签、序号和结论全部根据文章原文确定性排版；标题使用粗体、正文使用随 Worker 镜像安装的阿里巴巴普惠体 Regular，长文字按可用区域自动换行和缩放，官方极客跳动 Logo 原始资产固定在右上角。最终视觉统一使用白底、黑/深灰与克制红色，并采用内容匹配的多种信息结构。生产图片调用失败会明确失败，不会静默以固定模板冒充 AI 生图；模板仅保留给本地 mock 回归。公众号结尾改为统一节奏、顶部细分割线、底部精彩推荐，支持在复核稿中修改观察板块标题；组合封面使用真实白色 GeekDance 字标叠加顶部最高 50% 不透明度的红色渐变，不再渲染副标。
- 本地候选已新增普通文章真实封面工作区、公众号双比例拼图、确定性中文章节结构图、编辑器全文自适应和可维护公众号结尾；本地类型检查、95 项测试、生产构建、扩展安全检查与 UI 硬化扫描通过。尚未提交、推送或部署，线上状态不代表本候选结果。

## 2026-08-12 小红书多成员扩展补充

- 渠道管理已对运营成员开放；成员可下载公司版扩展、检测当前电脑、自动创建个人连接、查看最近使用时间并撤销自己的连接。
- 个人令牌由网站一次性传给扩展，不再要求运营人员手工复制；配对失败会撤销服务端新令牌并恢复扩展原配置。
- 扩展升级为 `1.1.1`，弹窗不再领取“下一条团队任务”；客户端和服务端都强制要求任务详情指定唯一任务 ID。连接检查能区分首次使用、授权过期和临时网络失败，避免网络抖动重复创建设备连接；草稿成功只接受本次页面变化产生的新信号，避免旧自动保存提示误报。
- 任务详情显示领取任务、下载配图、打开创作页、上传填写、完成或需人工核对等阶段；仍只保存草稿，禁止正式发布和不明确结果自动重试。
- 生产 Web 构建自动生成公司内部分发 ZIP，其他同事部署后可用自己的运营中心账号下载、安装和连接，不需要共享密码、Cookie 或扩展令牌。
- 本地验证：扩展安全检查通过；全仓 TypeScript 检查通过；内容引擎 49 项、渠道适配器 14 项、API 15 项、worker 16 项，共 94 项测试通过；生产 Web 构建和 ZIP 完整性检查通过。
- 同事操作已继续简化为“安装一次助手 → 登录小红书 → 在任务页点击保存草稿”；首次点击会自动建立电脑连接并继续执行，令牌、接口地址和版本判断全部隐藏在后台，渠道管理只保留安装、状态检查和停用设备能力。
- 尚未完成：当前改动未提交、未推送、未部署；小红书真实草稿保存仍需部署后在至少两台已登录 Chrome 上完成多成员生产验收，不能仅凭本地构建宣称线上闭环已经通过。

## 首次线上自检补充

- 线上 `/api/health`、登录页、人工复核页和扩展 ZIP 均返回 200；未登录访问受保护 API 返回 401。
- 修复扩展复用任意小红书创作标签页的问题，避免覆盖运营人员未保存内容。
- 为多图下载加入 60 秒超时与逐图任务续租，减少慢网络导致的错误过期复核。
- 扩展 API 地址在弹窗和后台双重限制为正式运营中心及明确的本地开发地址，避免配对令牌误发。
- 扩展升级为 `1.0.2`，网站与扩展增加版本握手，旧版本无法继续执行上传并会显示升级指引。
- 补齐 GeekHome、阿里云 OSS 与极客跳动子域图片读取权限，避免真实素材在扩展侧下载失败。
- 规范化图片 MIME 并保留 PNG/JPEG/WebP 文件扩展名；保存前等待可用的草稿按钮，避免图片仍在处理时误触发。
- 网站等待扩展完成的上限覆盖多图下载最坏时长；扩展弹窗异常后会恢复操作按钮，不再永久卡死。
- 任务详情只在首次载入时选择默认预览，轮询不再覆盖运营成员手动切换的渠道；离页后也不会遗留轮询。
- 人工复核列表改为串行定时刷新；处理记录和任务详情补充复核意见、实际复核人、处理时间及外部草稿信息。
- 人工复核列表最多返回最近 200 条，防止长期运行后一次加载全部历史记录。

## 2026-08-17 多账号投放影响回归

- 多账号投放使用独立的账号、批次和账号投放项，不覆盖原 `job_targets` 与原单账号浏览器上传任务；官网、公众号和既有小红书草稿状态机保持兼容。
- 投放项冻结创建批次时的目标账号标识和名称。后续同一扩展改绑其他账号时，旧任务不会被重定向；扩展检测到当前登录账号与冻结账号不一致会停止执行。
- 撤销或过期扩展连接会同步停用账号、取消尚未领取的投放项并重新汇总批次，账号列表区分在线、离线待连接和已停用。
- `filled` 只表示内容已填入编辑器，不再被汇总为草稿成功；没有唯一草稿按钮时进入人工处理，点击后没有明确成功信号时进入不确定状态且不自动重试。
- 多渠道同时通过人工复核时，每个 Worker 只发布指定的复核渠道，不再用旧整篇缓存覆盖另一渠道刚完成的修改；多账号投放标题和正式发布确认使用复核后的渠道标题。
- 小红书复核主动删除全部图片时会停在人工复核并返回 `BROWSER_DRAFT_IMAGES_MISSING`；保留已有配图后可正常生成扩展上传包。
- 本地验证通过：全仓 TypeScript 检查；内容引擎 59 项、渠道适配器 15 项、API 37 项、Worker 33 项，共 144 项单元测试；扩展安全检查；生产构建；临时 PostgreSQL、Redis、素材和账号环境下的隔离全流程回归。隔离回归覆盖原三渠道复核后草稿、旧小红书指定任务、扩展撤销、多账号隔离/幂等/部分失败/账号快照、正式发布标题与指纹门禁。
- 尚未完成：知乎、今日头条和百家号真实账号及真实页面仍需有账号后验收；当前验证不能替代生产平台 DOM、验证码和真实草稿/发布成功信号测试。改动尚未提交、推送或部署。
